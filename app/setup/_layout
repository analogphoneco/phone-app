import { Stack } from "expo-router";

export default function SetupLayout() {
  return (
    <Stack>
      <Stack.Screen name="index" options={{ title: "Setup" }} />
      <Stack.Screen name="create-line" options={{ title: "Create line" }} />
      <Stack.Screen name="pick-number" options={{ title: "Pick number" }} />
      <Stack.Screen name="provision" options={{ title: "Provision ATA" }} />
      <Stack.Screen name="status" options={{ title: "Status" }} />
    </Stack>
  );
}
